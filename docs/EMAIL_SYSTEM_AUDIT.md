# Email System Audit Notes

## Active provider

Little Learner Hub standardizes transactional mail on **Resend** when credentials and DNS are ready.

Recommended sender:

- **Little Learner Hub <support@littlelearnershubbyleah.com>**

Why:

- best fit for password resets, support replies, admin notifications, and billing/help replies
- professional and trustworthy for transactional mail
- avoids the dead-end experience of `noreply@...`

## Fail-closed auth email (important)

Server-owned password reset and email verification links are **fail-closed** until Resend is ready:

- `RESEND_API_KEY` must be set
- Outbound From must use the verified domain `littlelearnershubbyleah.com`
- Until then, production continues to use **Firebase Auth** (or existing demo/local paths)

Do not cut over the client to server-only reset/verification until DNS + Resend are verified in production.

SendGrid / Postmark remain supported fallbacks for general outbound mail (support tickets, engagement campaigns). Auth-link emails require Resend specifically.

## Render environment variables

Required for Resend transactional mail:

- `RESEND_API_KEY`
- `SUPPORT_EMAIL_FROM=Little Learner Hub <support@littlelearnershubbyleah.com>`
- `SUPPORT_EMAIL_TO=support@littlelearnershubbyleah.com`
- `SITE_URL=https://littlelearnershubbyleah.com` (or the final public app URL)

Optional:

- `RESEND_API_BASE_URL` — defaults to `https://api.resend.com` (tests may point at a fake Resend)

Marketing / re-engagement automations stay gated by `EMAIL_AUTOMATIONS_ENABLED` (default `false`) and are independent of auth-link email.

## DNS requirements for `littlelearnershubbyleah.com`

At minimum, Resend domain verification requires the exact DNS records shown in the Resend dashboard for the chosen sending domain/subdomain.

Typical records:

- SPF TXT record for the sending domain/subdomain
- DKIM TXT record(s) generated by Resend
- Resend MX record(s) required for bounce/complaint handling
- DMARC TXT record at `_dmarc.littlelearnershubbyleah.com`

Recommended DMARC starter record:

```txt
v=DMARC1; p=none; rua=mailto:support@littlelearnershubbyleah.com
```

After confirming SPF/DKIM pass in production, tighten policy gradually (`quarantine` then `reject`).

## Current public DNS audit snapshot

Observed during audit:

- `littlelearnershubbyleah.com` had Namecheap email-forwarding SPF
- a DKIM TXT existed at `resend._domainkey.littlelearnershubbyleah.com`
- **no DMARC TXT** was present at `_dmarc.littlelearnershubbyleah.com`

This means DNS still needs final verification against the exact Resend dashboard values before deliverability can be considered complete.

## Deferred from original repair PR

- Unconditional replacement of Firebase password reset / verification with server Resend flows
- Removing SendGrid / Postmark provider support
- Auto-sending verification email on every signup (conflicts with engagement gating and changes prod behavior without secrets readiness)
